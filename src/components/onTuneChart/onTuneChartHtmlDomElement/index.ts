import { TopResizeBar } from "./TopResizeBar";
import { RightResizeBar } from "./RightResizeBar";
import { BottomResizeBar } from "./BottomResizeBar";
import { LeftResizeBar } from "./LeftResizeBar";

export const ResizeBars = {
    TOP: TopResizeBar,
    RIGHT: RightResizeBar,
    BOTTOM: BottomResizeBar,
    LEFT: LeftResizeBar
};